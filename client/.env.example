# Copy to `.env` and fill in the values for your setup.

# Host running `server/` (the Bun/TypeScript WS bridge). Use "127.0.0.1" if the
# client and server run on the same machine.
COWORK_SERVER_IP="127.0.0.1"

# Port the WS server listens on (see `server/config.ts`).
COWORK_SERVER_PORT="8080"
